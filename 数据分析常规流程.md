1. 提出问题
2. 收集数据
+ 4种数据的来源：

1.观测和统计得到的数据

2.问卷和调研得到的数据

3.从数据库中获取的数据

4.从网络爬虫获取的数据

3. 数据处理和清洗

例子：
```python
# 2. 识别并处理异常值
# 使用布尔索引筛选出"订单量"这一列值里大于0且小于100000000的数据
data = data[(data["订单量"]>0) & (data["订单量"]<100000000)]
```

4. 数据分析
+ 图表可视化（描述性分析）
+ 找出数据大致分布状态，进行单个因素分析
+ 关联数据进行探索性分析
  +  探索性数据分析，是指仅有一些非常浅的假设，通过数据分析方法，深入探索数据。
  + 它有三大作用，包括分析现状、分析原因、预测未来。
  + 如果缺失一些基本的数据分析思路，那么面对处理好的数据，也会不知道从何入手。
  + 但其实，数据分析的思路非常简单。三大作用分别对应着三个基本思路：对比（直观地看出变化/差距，并且量化变化/差距是多少。）、细分、预测。
+ 常用的对比思路，就是同比和环比。



同比，通常是本期数据和过往的同一期数据的对比。



例如，2020二季度GDP的同比增长，是和2019二季度GDP对比，这就是年同比。可以消除不同季度的季节因素影响。



环比，通常是本期数据和连续周期的上一期数据的对比。



例如，2020二季度全国GDP的环比增长，是和2020一季度对比，这就是环比。可以观测数据连续的变化趋势。
+ 细分


在深入挖掘数据现状，和追溯内部原因的时候。需要在对比的基础上，进行细分分析。



细分，是指将数据划分成不同的部分，从而对比内部各个部分之间异同和关系的思路。

细分分析，可以对比量的直接大小外，还可以对比内部结构。



内部结构中，某个部分的比例越大，说明其重要程度越高，影响越大

+ 一次探索性分析的过程，通常是从问题出发，不断细分对比，从而发现有洞见和针对性的结论。

在单一的维度上，不断向下细分，进行维度下钻。

或者，在单一的维度上，不断添加新的维度，进行维度交叉。

可以说，细分是数据分析的本源。
<img width="862" height="1030" alt="image" src="https://github.com/user-attachments/assets/28efffcb-721e-4d14-b4b1-3abe795bba4b" />
<img width="1856" height="570" alt="image" src="https://github.com/user-attachments/assets/f18d6f4f-1a42-4361-ad9c-9e54137c425f" />

+ 预测


数据分析中，预测未来是一个非常重要的问题。数据之间客观上存在互相影响和依存的关系。



挖掘这种数据间的关系，就可以通过一个因素的发展，从而推断另一个因素的发展，这就实现了预测。
+ 预测，往往要通过更复杂的数学模型来实现。一般来说分为三种：



相关性分析



回归分析



时间序列模型
<img width="1386" height="1150" alt="NumPy和数组" src="https://github.com/user-attachments/assets/40d13448-ea7f-402c-811c-30a662e867dd" />
<img width="1837" height="1336" alt="Pandas和Series" src="https://github.com/user-attachments/assets/5750b0a4-d0fc-40b4-9f5a-8a25d676a501" />
<img width="2904" height="2378" alt="DataFrame" src="https://github.com/user-attachments/assets/9e134104-9078-49d5-ad98-656245dd77e0" />
<img width="1391" height="914" alt="文件读取" src="https://github.com/user-attachments/assets/d28361a0-ce9a-4011-8b76-a9f8cf4e208c" />
<img width="1391" height="654" alt="索引和数据筛选（上）" src="https://github.com/user-attachments/assets/22163ebe-f4e5-4e46-952e-0d9ab2cf3a30" />
<img width="1486" height="563" alt="6-50" src="https://github.com/user-attachments/assets/6cfbc6db-b6a5-439a-a5f9-e4223c295660" />

<img width="1704" height="585" alt="索引和数据筛选（下）" src="https://github.com/user-attachments/assets/c50a4ff5-8e9e-43ad-90db-f508f39cb820" />
在pandas模块中，逻辑运算符有以下几种：



& 表示并且

| 表示或者

~ 表示非
<img width="3340" height="3392" alt="格式转换和时间类型副本" src="https://github.com/user-attachments/assets/fb7e8a5b-fed4-4f38-ac0c-f66723b54274" />
+ 字符串：
<img width="1791" height="1734" alt="1" src="https://github.com/user-attachments/assets/a6f36054-7377-4d8a-a669-cb2dedb6287f" />
+ 时间格式：
<img width="1791" height="1734" alt="2" src="https://github.com/user-attachments/assets/60cb964a-d881-4ac8-9b73-e21df93eb838" />

<img width="1284" height="1541" alt="统计函数" src="https://github.com/user-attachments/assets/a86076ea-57a3-4be3-9ae6-b0e4ecb28152" />
<img width="1505" height="2249" alt="美食测评排行（上） (1)" src="https://github.com/user-attachments/assets/a8d1932d-cd04-45d3-86c7-a3c7fe72bfb5" />

+ 对商品「下单时间」进行格式转换 ，将 字符串数据转换为时间类型，并指定「下单时间」为index 。



然后使用groupby()函数进行分组并计算出销售量，同时使用resample()函数「按月采样」。



最后还原索引index。

```python
# 导入模块
import pandas as pd

# 读取csv文件
df2019= pd.read_csv("/Users/Lily/2019年下半年订单表.csv")
df2020= pd.read_csv("/Users/Lily/2020年上半年订单表.csv")
# 使用pd.concat()函数纵向合并df2019和df2020
dfYear= pd.concat([df2019,df2020])

# 将"下单时间"这列字符串类型的数据转化成时间类型并赋值给dfYear["下单时间"]
dfYear["下单时间"] = pd.to_datetime(dfYear["下单时间"])
# 将"下单时间"这列设置成新的索引并赋值给dfnewYear 
dfnewYear = dfYear.set_index("下单时间")
# 使用groupby()函数,将「订单数据表」按「商品ID」进行分组，「按月采样」计算出订单「数量」的和并赋值给dfSales
dfSales = dfnewYear["数量"].groupby(dfnewYear["商品ID"]).resample("M").sum()
# 还原dfSales的索引index并赋值给dfnewSales
dfnewSales = dfSales.reset_index()
# 打印输出dfnewSales
print(dfnewSales)
```


<img width="1464" height="901" alt="美食测评排行（下）" src="https://github.com/user-attachments/assets/3abcd81b-e856-461b-ba4d-141a66b4cc4e" />
<img width="1473" height="744" alt="视频会员订单（上）" src="https://github.com/user-attachments/assets/fe4cfd38-7509-4401-9e1e-7e774d6a1b3a" />
<img width="1904" height="688" alt="image" src="https://github.com/user-attachments/assets/e6e5c8fe-2224-4838-b6b7-f4d6d5828cec" />
<img width="1854" height="552" alt="image" src="https://github.com/user-attachments/assets/a7331856-dac2-4539-8b17-feca6df2d390" />

<img width="1492" height="512" alt="视频会员订单（中）" src="https://github.com/user-attachments/assets/9d284af0-50ff-4b38-9ec9-4317712ae98f" />
index =后面，需要传入需要删除的行数据的index属性，来指定“哪些行需要删除”的

```python
# 使用布尔索引和isnull函数，将payment_provider这一列的缺失值筛选出，赋值给变量dfPayNull
# dfPayNull就是，包含所有payment_provider这一列缺失值的行
dfPayNull = df[df['payment_provider'].isnull()]

# 使用drop函数，将包含所有payment_provider这一列缺失值的行删除
df.drop(index=dfPayNull.index, inplace = True)
```

+ 使用fillna()函数，将payment_provider这一列的缺失值，全部填充成了value。

<img width="1630" height="614" alt="视频会员订单（下）" src="https://github.com/user-attachments/assets/71e84145-defc-420f-9a2f-237bff1765dd" />

```python
# 导入pandas模块，简称为pd
import pandas as pd

# 读取路径为"/Users/clean/视频会员订单数据源.csv"的文件，赋值给变量df
df = pd.read_csv("/Users/clean/视频会员订单数据源.csv")

# 商品价格price，单位分转化成元
df['price'] = df['price'] /100

# 使用to_datetime()函数，将订单创建时间create_time和支付时间pay_time，转化成时间格式
df['create_time'] = pd.to_datetime(df['create_time'])
df['pay_time'] = pd.to_datetime(df['pay_time'])

# 处理缺失值

# 使用布尔索引和isnull函数，将payment_provider这一列的缺失值筛选出，赋值给变量dfPayNull
# dfPayNull就是，包含所有payment_provider这一列缺失值的行
dfPayNull = df[df['payment_provider'].isnull()]

# 使用drop函数，将包含所有payment_provider这一列缺失值的行删除
df.drop(index=dfPayNull.index, inplace = True)

# 使用布尔索引和isnull函数，将platform这一列的缺失值筛选出，赋值给变量dfPlatNull
# dfPlatNull就是，包含所有platform这一列缺失值的行
dfPlatNull = df[df['platform'].isnull()]

# 使用drop函数，将dfPlatNull，也就是包含所有platform这一列缺失值的行删除
df.drop(index=dfPlatNull.index,inplace = True)


# 处理异常值

# 使用布尔索引、isin()、~运算符，将price这一列不是25.00，68.00，248.00的异常值筛选出来，赋值给变量dfWrongPrice
dfWrongPrice = df[~df['price'].isin([25.00,68.00,248.00])]

# 使用drop函数，将dfWrongPrice，也就是price不是25.00，68.00，248.00的异常值的行删除
df.drop(index=dfWrongPrice.index,inplace = True)

# 使用布尔索引，将支付时间pay_time小于创建时间create_time的异常值筛选出来，赋值给变量dfWrongTime
dfWrongTime = df[df['pay_time'] < df['create_time']]

# 使用drop函数，将dfWrongTime，也就是支付时间pay_time小于创建时间create_time的异常值所在行删

df.drop(index = dfWrongTime.index ,inplace=True)

# 处理重复值

# 使用duplicated函数，将order_id这一列的重复值做判断，用print输出
print(df['order_id'].duplicated())
```

<img width="1611" height="1494" alt="可视化基础（Ⅰ）" src="https://github.com/user-attachments/assets/2973a2c8-0e0b-4734-b2d0-3bf46e4af665" />
<img width="1328" height="377" alt="可视化基础（Ⅱ）" src="https://github.com/user-attachments/assets/75141d29-0d76-45cb-8e46-bf30af05f4d9" />
<img width="937" height="373" alt="可视化基础（Ⅲ）" src="https://github.com/user-attachments/assets/45545ac0-378a-4cb3-93f1-f99a2a3069c4" />
<img width="1603" height="1318" alt="可视化基础（Ⅳ）" src="https://github.com/user-attachments/assets/cfeb27e8-e8b9-470f-a08d-6f484cd3b13d" />
<img width="3382" height="1813" alt="洞察房价陷阱（上）" src="https://github.com/user-attachments/assets/21ab0e85-1483-4661-ba07-14de93bfd1f8" />
<img width="1820" height="898" alt="image" src="https://github.com/user-attachments/assets/30fe8327-69f8-48ad-9bd7-e1f386340649" />
+ 由于直方图，体现的是数据区间的分布情况。因此，直方图和柱状图有如下区别：



1. 直方图x轴只能是数值型数据，而柱状图可以是"一班"、"二班"这样的类别型数据。



2.直方图是连续的图形分布，y轴是这一段区间内数量的总和。而柱状图是单一的柱形，y轴是这一个值对应的数量。
+ 直方图，需要用到的是plt.hist()函数。直接将一组数据作为参数传入，hist()函数能够自行进行分段统计，展示直方图
+ 设置bins=100，也就是设置100个统计区间。直方图就会发生如图的变化，区间更细，分布也就更清晰了
<img width="1239" height="1369" alt="洞察房价陷阱（下）" src="https://github.com/user-attachments/assets/1c2798f0-2e79-4137-a3eb-9f9a6093b21a" />
<img width="1593" height="761" alt="地产营业额分析（上）" src="https://github.com/user-attachments/assets/5cb84115-6046-4e11-8c9d-0a1d9af22d43" />
<img width="2388" height="1554" alt="地产营业额分析（中）" src="https://github.com/user-attachments/assets/421969a0-7650-407b-9478-ac1b85378437" />

```python
# 导入pandas模块，并以"pd"为该模块的简写
import pandas as pd

# 读取CSV文件，并将结果赋值给变量data
data = pd.read_csv("/Users/yequ/商场营业额数据.csv")

# 使用pd.to_datetime()函数，将"date"列的数据转化为时间格式
data["date"] = pd.to_datetime(data["date"])

# 使用groupby()函数，按照日期分组后求和
# 将结果赋值给变量groupByDate
groupByDate = data.groupby(data["date"]).sum()

# 使用resample()函数，将groupByDate修改为以月为单位的数据
# 并赋值给变量resampleData
resampleData = groupByDate.resample("M").sum()

# 输出resampleData变量
print(resampleData)
```
```python
# 导入pandas模块，并以"pd"为该模块的简写
import pandas as pd
# 导入matplotlib.pyplot，并使用"plt"作为该模块的简写
import matplotlib.pyplot as plt

# 通过 rcParams 参数将字体设置为 Arial Unicode MS 
plt.rcParams["font.sans-serif"] = "Arial Unicode MS"

# 使用pd.read_csv()函数读取路径为 "/Users/yequ/商场营业额数据.csv" 的CSV文件
# 并将结果赋值给变量data
data = pd.read_csv("/Users/yequ/商场营业额数据.csv")

# 使用pd.to_datetime()函数，将"date"列的数据转化为时间格式
data["date"] = pd.to_datetime(data["date"])

# 使用set_index()函数，把"date"列设置为index
data = data.set_index("date")

# 使用groupby()、resample()和sum()函数
# 计算每个业态下每个月的营业额总和
# 将结果赋值给变量groupByCategory
groupByCategory = data.groupby(data["category"]).resample("M").sum()

# 依次访问"娱乐"、"服务"、"零售"和"餐饮"对应的数据
enter = groupByCategory.loc["娱乐"]
service = groupByCategory.loc["服务"]
retail = groupByCategory.loc["零售"]
catering = groupByCategory.loc["餐饮"]

# 依次将enter、service、retail和catering变量中的index
# 转换为"%Y/%m"的格式
enter.index = enter.index.strftime("%Y/%m")
service.index = service.index.strftime("%Y/%m")
retail.index = retail.index.strftime("%Y/%m")
catering.index = catering.index.strftime("%Y/%m")

# 添加4个子图
# 选择序号为1的子图
plt.subplot(2,2,1)
# 以enter.index为x轴的值和enter["turnover"]为y轴的值，绘制柱状图
plt.bar(enter.index,enter["turnover"])
# 将x轴刻度旋转90度
plt.xticks(rotation = 90)
# 将图像标题设置为 "娱乐"
plt.title("娱乐")

# 选择序号为2的子图
plt.subplot(2,2,2)
# 以service.index为x轴的值和service["turnover"]为y轴的值，绘制柱状图
plt.bar(service.index,service["turnover"])
# 将x轴刻度旋转90度
plt.xticks(rotation = 90)
# 将图像标题设置为 "服务"
plt.title("服务")

# 选择序号为3的子图
plt.subplot(2,2,3)
# 以retail.index为x轴的值和retail["turnover"]为y轴的值，绘制柱状图
plt.bar(retail.index, retail["turnover"])
# 将x轴刻度旋转90度
plt.xticks(rotation = 90)
# 将图像标题设置为 "零售"
plt.title("零售")

# 选择序号为4的子图
plt.subplot(2,2,4)
# 以catering.index为x轴的值和catering["turnover"]为y轴的值，绘制柱状图
plt.bar(catering.index, catering["turnover"])
# 将x轴刻度旋转90度
plt.xticks(rotation = 90)
# 将图像标题设置为"餐饮"
plt.title("餐饮")

# 调整子图布局
plt.tight_layout()
# 使用plt.show()函数显示图像
plt.show()
```

<img width="1704" height="819" alt="地产营业额分析（下）" src="https://github.com/user-attachments/assets/43188f1d-6b22-4808-bbee-1d69b5c22a15" />

```python
# 导入pandas模块，并以"pd"为该模块的简写
import pandas as pd

# 使用pd.read_csv()函数读取路径为 "/Users/yequ/商场营业额数据.csv" 的CSV文件
# 并将结果赋值给变量data
data = pd.read_csv("/Users/yequ/商场营业额数据.csv")

# 使用pd.to_datetime()函数，将"date"列的数据转化为时间格式
data["date"] = pd.to_datetime(data["date"])

# 使用set_index()函数，把"date"列设置为index
data = data.set_index("date")

# 使用groupby()、resample()和sum()函数
# 计算每个业态下每个月的营业额总和
# 将结果赋值给变量groupByCategory
groupByCategory = data.groupby(data["category"]).resample("M").sum()

# 使用unstack()函数将groupByCategory按照"category"重新排列
groupByCategory = groupByCategory.unstack("category")

# 使用print()输出变量groupByCategory
print(groupByCategory)
```
+ 在pandas中，我们经常会使用apply()函数，将自定义的函数应用到一个DataFrame上，对其每一行或每一列进行指定的操作，然后根据自定义的函数返回一个新的结果。
+ 隐喻:
自定义的函数就像一个食品加工盒子，我们把每一列的数据当作原料传入到这个盒子里，按照盒子里定义的方式处理后，将结果返回。

+ 为了获取groupByCategory变量里每一列数据的当月营业额占比，Sasa首先在第26-27行，自定义了一个计算占比的函数：getPercentage()。

然后在第30行，对整个DataFrame对象，也就是groupByCategory变量，使用了apply()函数，并将自定义的函数名称：getPercentage作为参数传入到apply()函数里。

```python
# 导入pandas模块，并以"pd"为该模块的简写
import pandas as pd

# 使用pd.read_csv()函数读取路径为 "/Users/yequ/商场营业额数据.csv" 的CSV文件
# 并将结果赋值给变量data
data = pd.read_csv("/Users/yequ/商场营业额数据.csv")

# 使用pd.to_datetime()函数，将"date"列的数据转化为时间格式
data["date"] = pd.to_datetime(data["date"])

# 使用set_index()函数，把"date"列设置为index
data = data.set_index("date")

# 使用groupby()、resample()和sum()函数
# 计算每个业态下每个月的营业额总和
# 将结果赋值给变量groupByCategory
groupByCategory = data.groupby(data["category"]).resample("M").sum()

# 使用unstack()函数将groupByCategory按照"category"重新排列
groupByCategory = groupByCategory.unstack("category")

# 计算每月总营业额，并赋值给变量sumTurnover
sumTurnover = groupByCategory.sum(axis=1)

# 计算占比的函数
def getPercentage(item):
    return item/sumTurnover

# 获取各营业额占当月营业额的比例，并赋值给变量percentage
percentage = groupByCategory.apply(getPercentage)

# 使用print()输出变量percentage
print(percentage)
```


<img width="2367" height="2253" alt="axis2-3" src="https://github.com/user-attachments/assets/1bf033e2-f1b4-478d-876a-6151f41be5a8" />
***
+ 绘制百分比堆积图
<img width="1842" height="2291" alt="noxy" src="https://github.com/user-attachments/assets/32bea6dd-9673-4669-b2b1-ca930e2160a5" />

+  直接对DataFrame对象使用plot.bar()函数。

+  传入stacked=True时，每一行数值类型的值会被堆积到同一个柱子中，作为y轴的值，且会根据columns自动生成对应的图例
  
+  DataFrame里的index会被作为x轴的值，并且index的名称会自动变为x轴的标题


```python
# 导入matplotlib.pyplot，并使用"plt"作为该模块的简写
import matplotlib.pyplot as plt
# 导入pandas模块，并以"pd"为该模块的简写
import pandas as pd

# 通过给 plt.rcParams["font.sans-serif"] 赋值
# 将字体设置为 Arial Unicode MS 
plt.rcParams["font.sans-serif"] = "Arial Unicode MS"

# 使用pd.read_csv()函数读取路径为 "/Users/yequ/商场营业额数据.csv" 的CSV文件
# 并将结果赋值给变量data
data = pd.read_csv("/Users/yequ/商场营业额数据.csv")

# 使用pd.to_datetime()函数，将"date"列的数据转化为时间格式
data["date"] = pd.to_datetime(data["date"])

# 使用set_index()函数，把"date"列设置为index
data = data.set_index("date")

# 使用groupby()、resample()和sum()函数
# 计算每个业态下每个月的营业额总和
# 将结果赋值给变量groupByCategory
groupByCategory = data.groupby(data["category"]).resample("M").sum()

# 使用unstack()函数将groupByCategory按照"category"重新排列
groupByCategory = groupByCategory.unstack("category")

# 计算每月总营业额，并赋值给变量sumTurnover
sumTurnover = groupByCategory.sum(axis=1)

# 计算占比的函数
def getPercentage(item):
    return item / sumTurnover

# 根据groupByCategory变量、apply()函数和getPercentage
# 获取各营业额占当月营业额的比例，并赋值给变量percentage
percentage = groupByCategory.apply(getPercentage)

# 将percentage的index转换成"年-月"格式
percentage.index = percentage.index.strftime("%Y-%m")

# 使用plot.bar()函数
# 根据percentage变量，绘制百分比堆积柱状图
percentage.plot.bar(stacked=True)

# 使用plt.show()函数显示图像
plt.show()
```


<img width="1346" height="338" alt="母婴电商旗舰店（上）" src="https://github.com/user-attachments/assets/e405cedb-0627-4f83-a7a6-f940d8cfa753" />

```python
# 导入模块
import pandas as pd
# 读取csv文件
df2019= pd.read_csv("/Users/Lily/2019年下半年订单表.csv")
df2020= pd.read_csv("/Users/Lily/2020年上半年订单表.csv")
# 使用pd.concat()函数，纵向合并df2019、df2020并赋值给新的变量dfYear
dfYear= pd.concat([df2019,df2020])
# 打印输出dfYear
print(dfYear)
```
+ 外连接-outer

join参数是pd.concat()函数的一个可选参数，当没有指定时，默认值为"outer"，表示外连接。

书写格式为join = "outer"，也可以不写。

外连接更像是一种简单的堆叠，拥有相同列索引的合并成一列，不同列索引的单独成一列，同时所有列的值都会被保留，缺失的值用NaN填充补全。

<img width="1244" height="1008" alt="class14-1-1-4" src="https://github.com/user-attachments/assets/84d69459-c2ff-4d11-880f-063e1eed99b8" />


```python
# 导入模块
import pandas as pd
# 分别创建2个DataFrame并赋值给变量df1、df2
df1 = pd.DataFrame({"name": ["Peter", "Paul", "Mary"],"food": ["fish", "beans", "bread"]})
df2 = pd.DataFrame({"name": ["Mary", "Ben", "Peter"],"rank":[1, 2, 3]})
# 使用pd.concat()函数外连接，纵向合并df1、df2并赋值给新的变量df3
df3 = pd.concat([df1,df2],join="outer")
# 打印输出df1, df2, df3
print(df1)
print(df2)
print(df3)
```

+  内连接（inner）。

内连接在需要使用的时候必须指定，书写格式为join = "inner"。

内连接也是一种堆叠，不同的是，它只合并拥有相同列索引的列并保留它们的值，其余不同列索引的值全部丢弃。

```python
# 导入模块
import pandas as pd
# 分别创建2个DataFrame并赋值给变量df1、df2
df1 = pd.DataFrame({"name": ["Peter", "Paul", "Mary"],"food": ["fish", "beans", "bread"]})
df2 = pd.DataFrame({"name": ["Mary", "Ben", "Peter"],"rank":[1, 2, 3]})
# 使用pd.concat()函数内连接，纵向合并df1、df2并赋值给新的变量df4
df4 = pd.concat([df1,df2],join="inner")
# 打印输出df1, df2, df4
print(df1)
print(df2)
print(df4)
```


<img width="1150" height="1008" alt="class14-1-1-5" src="https://github.com/user-attachments/assets/d1466310-de8a-4a2d-a5e9-2a7d46a4082c" />




<img width="1865" height="420" alt="母婴电商旗舰店（下）" src="https://github.com/user-attachments/assets/11c2daf8-d74a-4d4c-bfbf-8715f62f8d7e" />
<img width="2862" height="1404" alt="class15-1-1-11" src="https://github.com/user-attachments/assets/fdfc0b1a-79c2-4b8d-82ae-e79d49602c33" />

+ 和pd.concat()函数不一样的是，pd.merge()函数在连接的时候是用参数how指定的。

+ 书写格式为how = "inner"

<img width="1208" height="838" alt="class15-1-1-7" src="https://github.com/user-attachments/assets/e7caafd8-045c-4dd2-ae9e-4b35a7f47aec" />

+ how = "outer"

<img width="1208" height="838" alt="class15-1-1-3" src="https://github.com/user-attachments/assets/c46f83b9-9443-4e7c-904b-b32b5960f65e" />

+ how = "left"

<img width="1208" height="838" alt="class15-1-1-4" src="https://github.com/user-attachments/assets/fdb9dc6c-258e-476b-a4d6-5316c97474bf" />

+ how = "right"

<img width="1208" height="838" alt="class15-1-1-5" src="https://github.com/user-attachments/assets/edb451f5-547f-4562-92e9-4b53fcce9f73" />

+ pd.merge()函数在连接时默认是自动寻找相同的列“Name”作为关联列进行连接的。

<img width="1208" height="686" alt="class15-20-1" src="https://github.com/user-attachments/assets/d7ff6ac3-8ac2-4f7b-b844-4a702f61567f" />

# 进阶
<img width="1692" height="1285" alt="便利店的销售数据分析" src="https://github.com/user-attachments/assets/5999b5a2-8f11-4abf-bde7-df176c01ebdb" />
<img width="4122" height="2304" alt="D2智能推荐与协同过滤" src="https://github.com/user-attachments/assets/b42b3b6e-7fa3-4b1f-a73e-2d1d4e00512a" />
<img width="1824" height="586" alt="image" src="https://github.com/user-attachments/assets/d3ae8167-5c33-429c-9f9e-d0ba751d77cd" />
<img width="5016" height="2070" alt="1P39" src="https://github.com/user-attachments/assets/e96f4317-13f8-42e2-be4e-df094b8a3213" />

```python
# 导入pandas模块，简称为pd
import pandas as pd  

# 使用read_csv()函数，sep参数，header参数和names参数
# 读取路径为"/Users/movie/ratings.csv"的文件
# 设置参数分隔符号sep="::"
# 传入参数header = None和names = ["用户id","电影id","评分","评分时间"]设置columns
# 将结果赋值给变量ratings
ratings = pd.read_csv("/Users/movie/ratings.csv",sep = "::",header = None,names = ["用户id","电影id","评分","评分时间"])

# 输出查看ratings
print(ratings)
```

<img width="1877" height="1464" alt="基于用户的协同过滤（上）" src="https://github.com/user-attachments/assets/316dbbe0-6a95-4af9-a8b6-f68eee822a80" />
<img width="1624" height="728" alt="image" src="https://github.com/user-attachments/assets/480d1e76-92e6-4c74-944f-9f367533d878" />

<img width="3594" height="2308" alt="基于用户的协同过滤（下）" src="https://github.com/user-attachments/assets/9152ae74-61d9-4eee-8450-6d13b9a76fe0" />
<img width="986" height="664" alt="4-32" src="https://github.com/user-attachments/assets/08dc297d-90c9-445c-8fca-2d65c78e57ee" />

<img width="2084" height="1245" alt="基于物品的协同过滤（上）" src="https://github.com/user-attachments/assets/599a2735-4cbe-458c-9c79-50cba3a16d7f" />

```python
# 导入pandas模块，简称pd
import pandas as pd

# 读取用户电影评分文件，并赋值给变量ratings
ratings = pd.read_csv("/Users/movie/ratings.csv")
# 读取电影信息文件，并赋值给变量movies
movies = pd.read_csv("/Users/movie/movies.csv") 
# 拼接ratings和movies，并赋值给变量result
result = pd.merge(ratings, movies) 

# 构建数据透视表，并赋值给变量user_movie
user_movie = result.pivot_table(index="用户id", columns="电影名", values="评分")

# 使用corr()函数，计算每两部电影之间的皮尔逊相关系数，并赋值给corrMatrix
corrMatrix = user_movie.corr(method="pearson")

# 输出corrMatrix进行查看
print(corrMatrix)
```
## 二、 寻找目标用户感兴趣的电影
<img width="1898" height="422" alt="image" src="https://github.com/user-attachments/assets/0bcdb5af-819f-491d-bf9d-8c3184fe3132" />

+ 获取user_movie中index为1的这一行里所有的非空数据，只需要：
1. 通过.loc属性，按照index的值，也就是【1】来访问行数据；

2. 使用dropna()函数，筛选出所有不是空值的数据，这时获取到的就是「用户1」评分过的所有电影数据。

```python
# 导入pandas模块，简称pd
import pandas as pd

'''一、寻找相似的电影'''

# 1. 读取并拼接数据集
ratings = pd.read_csv("/Users/movie/ratings.csv")
movies = pd.read_csv("/Users/movie/movies.csv") 
result = pd.merge(ratings, movies) 

# 2. 构建数据透视表
user_movie = result.pivot_table(index="用户id", columns="电影名", values="评分")

# 3. 计算每两部电影间的皮尔逊相关系数
corrMatrix = user_movie.corr(method="pearson")

'''二、寻找目标用户感兴趣的电影'''

# 获取「用户1」评分过的电影数据
user1Ratings = user_movie.loc[1].dropna()

# 输出user1_Ratings查看
print(user1Ratings)
```

<img width="1886" height="594" alt="image" src="https://github.com/user-attachments/assets/e9907f15-25e0-4863-9598-72c267dce851" />

<img width="2069" height="702" alt="基于物品的协同过滤（下）" src="https://github.com/user-attachments/assets/eb303454-25d7-4375-b8a1-5fa81b0b120c" />
<img width="816" height="656" alt="image" src="https://github.com/user-attachments/assets/20ede098-3ce1-4ede-a163-c297591d1a34" />
<img width="1283" height="1025" alt="5-15 (2)" src="https://github.com/user-attachments/assets/8df65f7e-4d8a-45e1-b1ad-3d2c9f198cae" />
<img width="1812" height="668" alt="image" src="https://github.com/user-attachments/assets/9aca5632-f466-4805-98a3-236c8fa4578d" />
<img width="1948" height="434" alt="image" src="https://github.com/user-attachments/assets/abf60d36-ed6b-45f2-a006-e48f3c21b67e" />
<img width="1926" height="470" alt="image" src="https://github.com/user-attachments/assets/046901ef-5ae4-4124-b8dc-4116160f4ef4" />
<img width="1128" height="791" alt="5-23 (1)" src="https://github.com/user-attachments/assets/397cb9b2-05cd-4917-afb5-d913de3fa47e" />
<img width="1924" height="384" alt="image" src="https://github.com/user-attachments/assets/e325cb75-d106-4184-975a-26c1ed8904cb" />

<img width="2090" height="438" alt="24 (2)" src="https://github.com/user-attachments/assets/a1422218-41f7-4d26-a8a8-399d03a0d13a" />
<img width="1908" height="552" alt="image" src="https://github.com/user-attachments/assets/613a8644-936f-4bf4-b071-c65b2ef91b4a" />

```python
# 导入pandas模块，简称pd
import pandas as pd

'''一、寻找相似的电影'''

# 1. 读取并拼接数据集
ratings = pd.read_csv("/Users/movie/ratings.csv")
movies = pd.read_csv("/Users/movie/movies.csv") 
result = pd.merge(ratings, movies) 

# 2. 构建数据透视表
user_movie = result.pivot_table(index="用户id", columns="电影名", values="评分")

# 3. 计算每两部电影间的皮尔逊相关系数
corrMatrix = user_movie.corr(method="pearson")

'''二、寻找目标用户感兴趣的电影'''

# 1. 获取「用户1」评分过的电影数据
user1Ratings = user_movie.loc[1].dropna()

# 2. 预测「用户1」对未评分电影的感兴趣程度
# 2.1 依次获取「用户1」评分过的电影的名称及评分
name = user1Ratings.index
score = user1Ratings.values
# 2.2 获取电影之间的皮尔逊相关系数
simsMovie = corrMatrix[name].drop(index=name)
# 2.3 计算分数与皮尔逊相关系数之间的乘积
prod = score * simsMovie
# 2.4 求和
movieList = prod.sum(axis=1)

'''三、针对目标用户和推荐规则，作出推荐'''

# 3.1 将推荐列表按照感兴趣程度降序排序
movieList = movieList.sort_values(ascending=False)

# 3.2 获取感兴趣程度最高的前5部电影
movieList = movieList.index[0:5]

# 3.3 获取可推荐电影的名称
movieList = movieList.values

# 输出movieList进行查看
print(movieList)
```

<img width="1272" height="791" alt="关联分析" src="https://github.com/user-attachments/assets/3ae043a7-5ba0-46b3-bf28-92dd6fcdc850" />
<img width="1524" height="1848" alt="浏览数据分析（上）" src="https://github.com/user-attachments/assets/8fbff61e-00cf-419b-850b-447c1050dbed" />

```python
import pandas as pd 
df = pd.read_csv("/Users/user_data/用户浏览数据.csv")

# 创建一个空列表articles
articles = []
# for循环遍历df['文章类型']
for i in df['文章类型']:
    # 用split()函数将i的数据拆分，并将返回的列表赋值给article
    article = i.split(',')
    # 用append()函数将article添加到空列表articles中
    articles.append(article)
    
# 导入apyori库中的apriori()函数
from apyori import apriori
# 用apriori()函数进行关联分析，将获取的关联规则赋值给rules
rules = apriori(articles,min_support=0.1,min_confidence=0.6)

# for循环遍历rules
for rule in rules:
    # 输出rule
    print(rule)
```

<img width="1515" height="984" alt="7-15" src="https://github.com/user-attachments/assets/2d099344-3a8b-4df7-a3e5-3765bbb202df" />


<img width="1831" height="2152" alt="9 浏览数据分析（下）" src="https://github.com/user-attachments/assets/1f152695-9666-401d-9863-0cc60350295e" />


```python
import pandas as pd 
df = pd.read_csv("/Users/user_data/用户浏览数据.csv")

# 创建一个空列表articles
articles = []
# for循环遍历df['文章类型']
for i in df['文章类型']:
    # 用split()函数将i的数据拆分，并将返回的列表赋值给article
    article = i.split(',')
    # 用append()函数将article添加到空列表articles中
    articles.append(article)
    
# 导入apyori库中的apriori()函数
from apyori import apriori
# 用apriori()函数进行关联分析，将获取的关联规则赋值给rules
rules = apriori(articles,min_support=0.1,min_confidence=0.6)

# 创建空列表，用于存储关联规则等数据
extract_result = []

# for循环遍历rules
for rule in rules:
    #  提取支持度，保留3位小数，并赋值给support
    support = round(rule.support,3)
    # for循环遍历ordered_statistics对象
    for i in rule.ordered_statistics:
        # 获取前件和后件，转为列表后，分别赋值给head_set和tail_set
        head_set = list(i.items_base)
        tail_set = list(i.items_add)
        # 跳过前件为空的数据
        if head_set == []:
            continue
        # 将前件、后件拼接成关联规则的形式，并赋值给related_category
        related_category = str(head_set) + '→' + str(tail_set)
        # 提取置信度，保留3位小数，并赋值给confidence
        confidence = round(i.confidence,3)
        # 提取提升度，保留3位小数，并赋值给lift
        lift = round(i.lift,3)
        # 将提取的数据保存到列表extract_result中
        extract_result.append([related_category, support, confidence, lift])
# 将数据转成DataFrame的形式，设置列名为'关联规则', '支持度', '置信度', '提升度'，并赋值给rule_data
rule_data = pd.DataFrame(extract_result, columns=['关联规则', '支持度', '置信度', '提升度'])
# 提取出提升度大于 1 的数据,并赋值给promoted_rules
promoted_rules = rule_data[rule_data['提升度']>1]
# 提取出提升度小于 1 的数据,并赋值给restricted_rules
restricted_rules = rule_data[rule_data['提升度']<1]

# 导入matplotlib.pyplot，并使用"plt"作为该模块的简写
import matplotlib.pyplot as plt
# 将字体设置为 Arial Unicode MS 
plt.rcParams["font.sans-serif"] = "Arial Unicode MS"
# 对promoted_rules变量使用plot.bar()函数绘制指定簇形柱状图
promoted_rules.plot.bar('关联规则',['支持度','置信度'],rot=0)
# 使用plt.title()函数，将图表标题设置为"促进关系的强关联规则"
plt.title("促进关系的强关联规则")
# 使用plt.show()函数显示图像
plt.show()
```

<img width="1771" height="1655" alt="用户分层模型（上）" src="https://github.com/user-attachments/assets/0d4baf41-67b0-4e27-82e8-afb5300fd8f5" />
<img width="1737" height="1118" alt="用户分层模型（中）" src="https://github.com/user-attachments/assets/4d8b05a8-9f6a-494e-961a-07e9e9fbd0ad" />
使用了pandas模块中的qcut()函数，将"time_gap"的数据以频率边界分箱，并设置均分为5组。
<img width="1856" height="1484" alt="用户分层模型（下）" src="https://github.com/user-attachments/assets/b10101b2-f38e-4c47-992b-c16a1e9da841" />
<img width="2379" height="2382" alt="101" src="https://github.com/user-attachments/assets/b49b882b-d9c0-4506-8054-a77cd28e900c" />
<img width="2030" height="644" alt="57 6" src="https://github.com/user-attachments/assets/ff0324a9-cee7-44fc-b889-b93dc1b2749c" />

```python
# 导入pandas模块
import pandas as pd 

'''获取描绘R、F、M的数据'''
# 1. 读取并处理数据集
# 读取文件
df = pd.read_csv("/Users/user_info/user_info.csv")
# 数据类型转换
df["last_order_date"] = pd.to_datetime(df["last_order_date"])

# 2. 获取描绘R的数据
# 导入datetime模块中的datetime
from datetime import datetime
# 使用datetime()函数，构建2019年4月1日的时间，赋值给endTime
endTime = datetime(2019,4,1)
# 计算endTime和"last_order_date"这一列的时间间隔
df["time_gap"] = endTime - df["last_order_date"]
# 通过.dt.days，将"time_gap"列的天数提取出来
df["time_gap"] = df["time_gap"].dt.days

'''依次划分R、F、M'''
# 1. 划分R
# 使用qcut()函数，将"time_gap"的数据分箱
# 均分为5组，区间标记命名为5-1，赋值给df["R"]
df["R"] = pd.qcut(df["time_gap"],q=5,labels=[5,4,3,2,1])

# 2. 划分F
# 使用qcut()函数，将"order_count"的数据分箱
# 均分为5组，区间标记命名为1-5，赋值给df["F"]
df["F"] = pd.qcut(df["order_count"],q=5,labels=[1,2,3,4,5])

# 3. 划分M
# 使用qcut()函数，将"total_amount"的数据分箱
# 均分为5组，区间标记命名为1-5分，赋值给df["M"]
df["M"] = pd.qcut(df["total_amount"],q=5,labels=[1,2,3,4,5])

'''对用户标记分层结果'''
# 1. 简化分值
# 定义一个函数rfmTrans，如果参数x>3，就返回1；否则，就返回0
def rfmTrans(x):
    if x>3:
        return 1
    else:
        return 0

# 对R、F、M这三列数据，分别使用apply()函数
# 将函数名rfmTrans作为参数传入，并分别重新赋值给R、F、M这三列
df["R"] = df["R"].apply(rfmTrans)
df["F"] = df["F"].apply(rfmTrans)
df["M"] = df["M"].apply(rfmTrans)

# 2. 获取数值标签
# 用astype()函数将R、F、M这三列转化为字符串格式
# 再用"+"把字符串拼接在一起，组成一个新的列"mark"
df["mark"] = df["R"].astype(str)+df["F"].astype(str)+df["M"].astype(str)

# 3. 标记用户层级
# 定义一个函数rfmType，将数据标签，转化为对应的用户分层
def rfmType(x):
    if x=="111":
        return "高价值用户"
    elif x=="101":
        return "重点发展用户"
    elif x=="011":
        return "重点唤回用户"
    elif x=="001":
        return "重点潜力用户"
    elif x=="110":
        return "一般潜力用户"
    elif x=="100":
        return "一般发展用户"
    elif x=="010":
        return "一般维系用户"
    else:
        return "低价值用户"

# 对"mark""列，使用apply()函数
# 将函数名rfmType作为参数传入，并将结果赋值给df["customer_type"]
df["customer_type"] = df["mark"].apply(rfmType)

# 4. 可视化结果
# 4.1 计算每类用户总量
# 使用groupby()函数
# 将df["customer_type"]按照df["customer_type"]进行分组
# 然后使用count()函数进行聚合，赋值给变量df_type
df_type = df["customer_type"].groupby(df["customer_type"]).count()

# 获取每个层级的用户总数
total_user = df_type.values

# 4.2 计算每类用户占比
df_perc = df_type/51394

# 4.3数据可视化
# 导入matplotlib.pyplot，并使用"plt"作为该模块的简写
import matplotlib.pyplot as plt

# 通过给 plt.rcParams["font.sans-serif"] 赋值
# 将字体设置为 Arial Unicode MS 
plt.rcParams["font.sans-serif"] = "Arial Unicode MS"

# 使用plt.bar()函数，绘制展示各层级用户总量的柱状图
# 以df_type.index为x轴的值df_type.values为y轴的值
# 将柱子的颜色设置为"skyblue"
plt.bar(df_type.index, df_type.values, color="skyblue")
# 使用plt.xlabel()函数，将x轴标题设置为"用户分层类别"
plt.xlabel("用户分层类别")
# 使用plt.ylabel()函数，将y轴标题设置为"各层级用户总数"
plt.ylabel("各层级用户总数")

# 使用plt.twinx()函数，添加另一个y轴
plt.twinx()

# 使用plt.plot()函数，绘制展示各层级用户占比的折线图
# 以df_perc.index为x轴的值，df_perc.values为y轴的值
# "o"作为标记点的样式，将折线颜色设置为"lightcoral"
plt.plot(df_perc.index, df_perc.values, marker="o", color="lightcoral")
# 使用plt.xlabel()函数，将x轴标题设置为"用户分层类别"
plt.xlabel("用户分层类别")
# 使用plt.ylabel()函数，将y轴标题设置为"各层级用户总数占比"
plt.ylabel("各层级用户总数占比")

# 使用plt.show()函数显示图像
plt.show()
```

<img width="1243" height="1861" alt="认识机器学习" src="https://github.com/user-attachments/assets/20f6cded-9e3e-4c2b-a662-ef26e9d2e9de" />
<img width="1311" height="1533" alt="14 一元线性回归" src="https://github.com/user-attachments/assets/db7c723c-f2b7-41bf-b9b8-d90a6f530b47" />

```python
import pandas as pd 
df = pd.read_csv("/Users/ad/exposure_to_new.csv")

# 导入sklearn.linear_model模块中的LinearRegression类
from sklearn.linear_model import LinearRegression

# 以二维结构读取"exposure"这一列，作为自变量x
x = df[["exposure"]]
# 以二维结构读取"new_user"这一列，作为因变量y
y = df[["new_user"]]

# 使用LinearRegression()初始化模型，赋值给lr
lr = LinearRegression()
# 使用自变量x和因变量y，训练线性回归模型lr
lr.fit(x,y)

# 使用coef_[0][0],获取系数b的值
b = lr.coef_[0][0]
# 使用intercept_[0],获取截距a的值
a = lr.intercept_[0]
# 输出a+b*200000的值
print(a+b*200000)
```

<img width="1721" height="2298" alt="15 多重线性回归（上）" src="https://github.com/user-attachments/assets/76da162e-0382-4447-a770-3d3d62d41003" />

```python
import pandas as pd 
df = pd.read_csv("/Users/mul/multiple_to_new.csv")
# 导入matplotlib.pyplot，简称plt
import matplotlib.pyplot as plt

# 导入seaborn模块，简称sns
import seaborn as sns
# 使用sns的pairplot函数，绘制多变量散点图，df作为参数传入
sns.pairplot(df)

# 使用plt.show()函数显示图像
plt.show()
```

<img width="1182" height="788" alt="image" src="https://github.com/user-attachments/assets/e56d4a26-a9ae-4ffd-a28a-740854924118" />
<img width="1094" height="718" alt="image" src="https://github.com/user-attachments/assets/aa32e05c-2d8d-485f-bab1-8caa920235f0" />


```python
import pandas as pd 
df = pd.read_csv("/Users/mul/multiple_to_new.csv")

# 将名为"date"的列的数据删除
df.drop("date", axis=1)

# 导入seaborn模块，简称sns
import seaborn as sns
# 导入matplotlib.pyplot，简称plt
import matplotlib.pyplot as plt

# 对df计算相关性，并赋值给变量corr
corr = df.corr()

# 使用sns的heatmap函数，绘制相关系数热力图
sns.heatmap(corr, cmap = "RdBu", square=True, annot = True)

# 使用plt.show()函数显示图像
plt.show()
```
## 建立回归模型
```python
import pandas as pd 
df = pd.read_csv("/Users/mul/multiple_to_new.csv")

# 以二维结构读取"exposure","hot","search"这三列，作为自变量x
x = df[["exposure", "hot", "search"]]
# 以二维结构读取"new_user"，作为因变量y
y = df[["new_user"]]

# 导入sklearn.linear_model模块中的LinearRegression函数
from sklearn.linear_model import LinearRegression

# 使用LinearRegression()初始化模型，赋值给lr_model
lr_model = LinearRegression()
# 使用lr_model模型的fit()函数，训练模型
lr_model.fit(x,y)

# 使用coef_[0][0],获取系数b的值，并保留4位小数
b = lr_model.coef_[0][0].round(4)
# 使用coef_[0][1],获取系数c的值，并保留4位小数
c = lr_model.coef_[0][1].round(4)
# 使用coef_[0][2],获取系数d的值，并保留4位小数
d = lr_model.coef_[0][2].round(4)
# 使用intercept_[0],获取截距a的值，并保留2位小数
a = lr_model.intercept_[0].round(2)
# 格式化输出，f"该线性回归模型为：Y={a}+{b}X1+{c}X2+{d}X3"
print(f"该线性回归模型为：Y={a}+{b}X1+{c}X2+{d}X3")
```
## 检验回归模型
根据输出的结果，多重线性回归的模型为：

Y=-7004.53+0.0049X1+1.0058X2+0.0132X3



完成模型的训练后，还是使用模型的score函数，对回归模型进行检验打分。
```python
import pandas as pd 
df = pd.read_csv("/Users/mul/multiple_to_new.csv")

# 以二维结构读取"exposure","hot","search"这三列，作为自变量x
x = df[["exposure", "hot", "search"]]
# 以二维结构读取"new_user"，作为因变量y
y = df[["new_user"]]

# 导入sklearn.linear_model模块中的LinearRegression函数
from sklearn.linear_model import LinearRegression

# 使用LinearRegression()初始化模型，赋值给lr_model
lr_model = LinearRegression()
# 使用lr_model模型的fit()函数，训练模型
lr_model.fit(x,y)

# 将x,y传入score( )函数，对模型打分,获取判定系数r2
r2 = lr_model.score(x,y)
# 输出r2
print(r2)
```
也就是说曝光量、搜索热度、平台搜索量和新增用户量为因变量的回归模型：

Y=-7004.53+0.0049X1+1.0058X2+0.0132X3

﻿

判定系数为0.94，非常接近1，模型精度足够高，我们可以用它进行简单的预测
## 利用模型预测

```python
import pandas as pd 
df = pd.read_csv("/Users/mul/multiple_to_new.csv")

# 以二维结构读取"exposure","hot","search"这三列，作为自变量x
x = df[["exposure", "hot", "search"]]
# 以二维结构读取"new_user"，作为因变量y
y = df[["new_user"]]

# 导入sklearn.linear_model模块中的LinearRegression函数
from sklearn.linear_model import LinearRegression

# 使用LinearRegression()初始化模型，赋值给lr_model
lr_model = LinearRegression()
# 使用lr_model模型的fit()函数，训练模型
lr_model.fit(x,y)

# 曝光量，搜索热度，关键词搜索量依次为300000,10000,30000
# 将300000,10000,30000以二维结构传入传入predict()函数进行预测，并赋值给 y_predict
y_predict = lr_model.predict([[300000,10000,30000]])

# 输出预测结果y_predict
print(y_predict)
```
***
在实际应用场景中，因变量和自变量存在相关关系，而自变量与自变量之间也有可能存在相关关系，有时还会是强相关关系。



也就是说，多个自变量的情况下，由于自变量的相互影响，有可能会导致模型的拟合效果并不好。



那么，我们如何检验多重线性回归模型是否存在以上问题呢？我们又该如何处理呢？
***
<img width="1776" height="2260" alt="16 多重线性回归（下）" src="https://github.com/user-attachments/assets/fbc903a6-24ec-4a8f-9efc-12e7155638bf" />
在多重线性回归中，可能存在多重共线性问题（多个自变量的情况下，由于自变量的相互影响，有可能会导致模型的拟合效果并不好）。
+ 关于多重共线性的两种判别方法：相关系数判断、方差膨胀系数法。
1. 相关系数判断

既然，不同自变量之间存在高度的线性相关关系，就会导致多重共线性。

那么，我们可以先通过corr()函数，计算出自变量之间的相关系数，来初步判断是否存在多重共线性。
```python
import pandas as pd 
df = pd.read_csv("/Users/mul/multiple_to_new.csv")

# 以二维结构读取"exposure","hot","search"这三列，作为自变量x
x = df[["exposure", "hot", "search"]]
# 以二维结构读取"new_user"，作为因变量y
y = df[["new_user"]]

# 使用corr()，计算自变量之间的相关系数，并输出
print(x.corr())
```
+ 相关系数只是多重共线性的充分条件，不是必要条件。

即自变量间的相关系数高，一定存在多重共线性。

但是，相关系数较小（比如，图中的0.5）的自变量之间，也有可能存在较为严重的多重共线性。

为了更加严谨，我们通常会在实战中，使用方差膨胀系数法，来进一步确定多重共线性的情况

2. 方差膨胀系数法

方差膨胀系数法，简称VIF检验。

+ 通过for循环遍历自变量x的列索引。
+ x.values作为参数传入，表示自变量x的数据
+ x.columns.get_loc(i)表示返回i所在列的列序号数字
+ 即自变量﻿"exposure", "hot", "search"所在的列，依次返回0,1,2。
+ 中括号表示将求得的方差膨胀系数的结果放入列表中
```python
import pandas as pd 
df = pd.read_csv("/Users/mul/multiple_to_new.csv")

# 以二维结构读取"exposure","hot","search"这三列，作为自变量x
x = df[["exposure", "hot", "search"]]
# 以二维结构读取"new_user"，作为因变量y
y = df[["new_user"]]

# 从statsmodels.stats.outliers_influence模块中导入variance_inflation_factor()函数
from statsmodels.stats.outliers_influence import variance_inflation_factor

# 通过for循环依次求得每个自变量的方差膨胀系数，并将结果放入列表中，赋值给变量vif
vif = [variance_inflation_factor(x.values, x.columns.get_loc(i)) for i in x.columns]

# 输出vif
print(vif)
```
+ 3个自变量依次为exposure, hot, search。



根据前面的输出：

exposure的VIF值为32；hot的VIF值为136；

search的VIF值为176；



也就是说，自变量hot, search的VIF值均大于100，说明多重共线性十分严重，我们要进行处理

+ 也就是说，我们已经确定了自变量hot, search存在严重的多重共线性问题。

﻿

如果我们还是利用全部数据集，来处理多重共线性，模型很容易出现：过拟合。



即，对数据集的准确度很高，对外部数据准确度较低。



+ 我们不妨先通过把数据集划分的方式，先来处理过拟合的问题。(对数据集的拟合效果好，但对数据集外的拟合效果差)。

简单来说，我们只要有两份数据集，一份用于训练模型，一份用于测试模型的效果，在一定程度上，就可以避免模型的过拟合问题。



还有一种更简单的方式，就是我们直接将单个数据集拆分为两个部分，这个过程就是划分训练集和测试集

```python
import pandas as pd 
df = pd.read_csv("/Users/mul/multiple_to_new.csv")

# 以二维结构读取"exposure","hot","search"这三列，作为自变量x
x = df[["exposure", "hot", "search"]]
# 以二维结构读取"new_user"，作为因变量y
y = df[["new_user"]]

# 导入sklearn.linear_model模块中的LinearRegression函数
from sklearn.linear_model import LinearRegression

# 导入sklearn.model_selection模块中的train_test_split函数
from sklearn.model_selection import train_test_split

# 使用train_test_split()函数划分训练集和测试集，设置参数test_size=0.2，random_state=1
# 依次赋值给x_train,x_test,y_train,y_test
x_train,x_test,y_train,y_test = train_test_split(x,y,test_size=0.2,random_state=1)

# 初始化线性回归模型，赋值给lr_model
lr_model = LinearRegression()
# 使用fit()函数训练模型,传入训练集数据
lr_model.fit(x_train,y_train)

# 使用score函数，传入测试集数据，得到模型的判定系数，并输出
print(lr_model.score(x_test,y_test))
```

<img width="1884" height="522" alt="image" src="https://github.com/user-attachments/assets/2de42504-997f-4b2f-aec7-1e0deb061e80" />

+ 训练集来建立新的模型
```python
import pandas as pd 
df = pd.read_csv("/Users/mul/multiple_to_new.csv")

# 以二维结构读取"exposure","hot","search"这三列，作为自变量x
x = df[["exposure", "hot", "search"]]
# 以二维结构读取"new_user"，作为因变量y
y = df[["new_user"]]

# 导入sklearn.linear_model模块中的LinearRegression函数
from sklearn.linear_model import LinearRegression

# 导入sklearn.model_selection模块中的train_test_split函数
from sklearn.model_selection import train_test_split

# 使用train_test_split()函数划分训练集和测试集，设置参数test_size=0.2，random_state=1
# 依次赋值给x_train,x_test,y_train,y_test
x_train,x_test,y_train,y_test = train_test_split(x,y,test_size=0.2,random_state=1)

# 初始化线性回归模型，赋值给lr_model
lr_model = LinearRegression()

# 使用fit()函数训练模型,传入训练集数据
lr_model.fit(x_train,y_train)

# 使用score函数，传入测试集数据，得到模型的判定系数，赋值给r2
r2 = lr_model.score(x_test,y_test)

# 输出r2
print(r2)

# 使用coef_[0][0],获取系数b的值，并保留4位小数
b = lr_model.coef_[0][0].round(4)
# 使用coef_[0][1],获取系数c的值，并保留4位小数
c = lr_model.coef_[0][1].round(4)
# 使用coef_[0][2],获取系数d的值，并保留4位小数
d = lr_model.coef_[0][2].round(4)
# 使用intercept_[0],获取截距a的值，并保留2位小数
a = lr_model.intercept_[0].round(2)
# 格式化输出，f"线性回归模型为：Y={a}+{b}X1+{c}X2+{d}X3"
print(f"线性回归模型为：Y={a}+{b}X1+{c}X2+{d}X3")
```
+ 总而言之，划分训练集和测试集，在某种程度上避免了模型的过拟合问题，加强了模型的泛化能力(对未知样本的适应能力)

***
<img width="915" height="965" alt="16-39" src="https://github.com/user-attachments/assets/ddc1af71-cfaa-4fd2-b6a5-dbb06e51673a" />

```python
import pandas as pd 
df = pd.read_csv("/Users/mul/multiple_to_new.csv")

# 以二维结构读取"exposure","hot","search"这三列，作为自变量x
x = df[["exposure", "hot", "search"]]
# 以二维结构读取"new_user"，作为因变量y
y = df[["new_user"]]

# 从statsmodels.stats.outliers_influence模块中导入variance_inflation_factor()函数
from statsmodels.stats.outliers_influence import variance_inflation_factor

# 使用drop()函数，设置参数columns，删除"hot"这一列，再赋值给x
x = x.drop(columns="hot")

# 通过for循环依次求得每个自变量的方差膨胀系数，并将结果放入列表中，赋值给变量vif
vif = [variance_inflation_factor(x.values, x.columns.get_loc(i)) for i in x.columns]

# 输出vif
print(vif)
```

***
+ 严重的多重共线性，通常会对判定系数、系数或截距两个方面产生影响。



我们只需要查看，处理模型前后，这2个维度是否有较好的改变。

```python
import pandas as pd 
df = pd.read_csv("/Users/mul/multiple_to_new.csv")

# 以二维结构读取"exposure","hot","search"这三列，作为自变量x
x = df[["exposure", "hot", "search"]]
# 以二维结构读取"new_user"，作为因变量y
y = df[["new_user"]]

# for循环遍历列表["hot","search"]
for i in ["hot","search"]:
    # 使用drop()函数，设置参数columns，删除i所在列，再赋值给xi
    xi = x.drop(columns=i)

    # 导入sklearn.model_selection模块中的train_test_split函数
    from sklearn.model_selection import train_test_split
    # 使用train_test_split()函数划分训练集和测试集，设置参数test_size=0.2，random_state=1
    # 依次赋值给x_train,x_test,y_train,y_test
    x_train,x_test,y_train,y_test=train_test_split(xi,y,test_size=0.2,random_state=1)

    # 导入sklearn.linear_model模块中的LinearRegression函数
    from sklearn.linear_model import LinearRegression
    # 初始化线性回归模型，赋值给lr_model
    lr_model = LinearRegression()
    # 使用fit()函数训练模型,传入训练集数据
    lr_model.fit(x_train,y_train)

    # 使用score函数，传入测试集数据，得到模型的判定系数，并输出
    print(lr_model.score(x_test,y_test))

    # 使用coef_[0][0],获取系数b的值，并保留4位小数
    b = lr_model.coef_[0][0].round(4)
    # 使用coef_[0][1],获取系数c的值，并保留4位小数
    c = lr_model.coef_[0][1].round(4)
    # 使用intercept_[0],获取截距a的值，并保留2位小数
    a = lr_model.intercept_[0].round(2)
    # 格式化输出f"自变量为{xi.columns.values}时:"
    print(f"自变量为{xi.columns.values}时:")
    # 格式化输出，f"对应的线性回归模型为：Y={a}+{b}X1+{c}X2"
    print(f"对应的线性回归模型为：Y={a}+{b}X1+{c}X2")
```
<img width="1864" height="710" alt="image" src="https://github.com/user-attachments/assets/ef5ccdeb-6286-4e67-b642-1521e8488f6b" />

<img width="1232" height="2069" alt="决策树模型 (2)" src="https://github.com/user-attachments/assets/525d75aa-ce73-4594-9a91-3a373bf33a02" />
+ 基尼系数表示，从系统中随机抽取两个样本，其类别不一样的概率。

并且，基尼系数越小，系统的区分度就越高，越适合用于分类预测
<img width="1844" height="646" alt="image" src="https://github.com/user-attachments/assets/6675b8c2-b7a0-4808-8d42-e4ba57ba75e9" />
<img width="1840" height="674" alt="image" src="https://github.com/user-attachments/assets/e6a4e991-9b75-463f-a994-eb8e4fe0f384" />

1. 未划分时系统的基尼系数为0.48，

2. 以「满意度 < 5」划分后，基尼系数为0.3，

3. 以「收入 < 10000」划分后，基尼系数为0.45。



我们的口号是﻿：﻿“基尼系数越小，分类效果越好”。



所以，这里我们选择特征「满意度 < 5」作为根节点进行分类最佳。
***

```python
import pandas as pd
df = pd.read_csv("/Users/sample/员工离职数据样本.csv")

# 导入sklearn.tree模块中的分类决策树模型DecisionTreeClassifier
from sklearn.tree import DecisionTreeClassifier

# 以二维结构读取"工资(k)","满意度"这两列，作为自变量x
x = df[["工资(k)","满意度"]]
# 以二维结构读取"离职"列，作为因变量y
y = df[["离职"]]

# 使用DecisionTreeClassifier()初始化模型
# 并设置参数max_depth=2,random_state=1，赋值给model
model = DecisionTreeClassifier(max_depth=2,random_state=1)
# 使用fit()函数训练模型
model.fit(x, y)

# 使用predict()函数，传入参数[[8,6]]进行预测，并赋值给y_pre
y_pre = model.predict([[8, 6]])

# 输出y_pre
print(y_pre)
```

<img width="1484" height="1396" alt="员工离职预测（上）" src="https://github.com/user-attachments/assets/ca3b7d36-6b6a-48c8-a285-4f0df129d5de" />
## 模型准确率

```python
import pandas as pd 
df = pd.read_csv("/Users/e_information/员工信息表.csv")

# 使用replace()将"工资"列的低"，"中"，"高"替换为0，1，2
df["工资"] = df["工资"].replace({'低': 0, '中': 1, '高': 2})

# 使用drop()函数删除"离职"列，剩余的数据作为自变量x
x = df.drop(columns="离职")

# 以二维结构读取"离职"列，作为因变量y
y = df[["离职"]]

# 导入sklearn.model_selection模块中的train_test_split函数
from sklearn.model_selection import train_test_split
# 使用train_test_split()函数划分训练集和测试集
x_train,x_test,y_train,y_test=train_test_split(x,y,test_size=0.2,random_state=123)

# 导入sklearn.tree模块中的分类决策树模型DecisionTreeClassifier
from sklearn.tree import DecisionTreeClassifier
# 使用DecisionTreeClassifier()初始化模型
# 并设置参数max_depth=3,random_state=123，赋值给model
model = DecisionTreeClassifier(max_depth=3,random_state=123)
# 使用fit()函数训练模型
model.fit(x_train, y_train)

# 将x_test传入使用predict()函数预测，将结果赋值给y_pred
y_pred = model.predict(x_test)

# 导入sklearn.metrics模块中的准确率函数accuracy_score
from sklearn.metrics import accuracy_score
# 使用accuracy_score()函数计算准确率，并赋值给score
score = accuracy_score(y_pred,y_test,normalize = True)
# 输出score
print(score)
```

## 读取数组的列数据
+ 中括号内，逗号前的数据表示行。这里冒号“:”，表示所有行
+ 中括号内，逗号后的数据表示列。因为索引都是从0开始的，所以这里的数字1表示第2列数据
```python
import pandas as pd 
df = pd.read_csv("/Users/e_information/员工信息表.csv")

# 使用replace()将"工资"列的低"，"中"，"高"替换为0，1，2
df["工资"] = df["工资"].replace({'低': 0, '中': 1, '高': 2})

# 使用drop()函数删除"离职"列，剩余的数据作为自变量x
x = df.drop(columns="离职")

# 以二维结构读取"离职"列，作为因变量y
y = df[["离职"]]

# 导入sklearn.model_selection模块中的train_test_split函数
from sklearn.model_selection import train_test_split
# 使用train_test_split()函数划分训练集和测试集
x_train,x_test,y_train,y_test=train_test_split(x,y,test_size=0.2,random_state=123)

# 导入sklearn.tree模块中的分类决策树模型DecisionTreeClassifier
from sklearn.tree import DecisionTreeClassifier
# 使用DecisionTreeClassifier()初始化模型
# 并设置参数max_depth=3,random_state=123，赋值给model
model = DecisionTreeClassifier(max_depth=3,random_state=123)
# 使用fit()函数训练模型
model.fit(x_train, y_train)

# 将x_test传入使用predict_proba()函数预测，将结果赋值给y_pred_proba
y_pred_proba = model.predict_proba(x_test)
# 输出y_pred_proba的第2列数据
print(y_pred_proba[:,1])
```


<img width="1271" height="2377" alt="员工离职预测（下）" src="https://github.com/user-attachments/assets/e7612aee-e2a7-46dd-a8e9-ff2445e838c4" />
## ROC曲线和AUC值
对于离职这种二分类的模型，我们不仅关心预测准确率，更关心命中率和误判率这两个指标。



对于这种二分类模型，主流的评估方法就是ROC曲线。



因为，ROC曲线就是以误判率为横坐标，命中率为纵坐标绘制的曲线。
<img width="459" height="366" alt="20-13" src="https://github.com/user-attachments/assets/d974baa5-f7d1-4e3b-af71-152dc6dd342c" />

+ roc_curve()函数，用于返回包含3个元素的元组
+ y_test是 测试集的因变量；
+ y_pred_proba[:,1]是 预测的离职概率
+ 赋值的变量：fpr，是误判率； tpr，是命中率； thres，是阈值。
```python
import pandas as pd 
df = pd.read_csv("/Users/e_information/员工信息表.csv")

# 使用replace()将"工资"列的低"，"中"，"高"替换为0，1，2
df["工资"] = df["工资"].replace({'低': 0, '中': 1, '高': 2})
# 使用drop()函数删除"离职"列，剩余的数据作为自变量x
x = df.drop(columns="离职")

# 以二维结构读取"离职"列，作为因变量y
y = df[["离职"]]

# 导入sklearn.model_selection模块中的train_test_split函数
from sklearn.model_selection import train_test_split
# 使用train_test_split()函数划分训练集和测试集
x_train,x_test,y_train,y_test=train_test_split(x,y,test_size=0.2,random_state=123)

# 导入sklearn.tree模块中的分类决策树模型DecisionTreeClassifier
from sklearn.tree import DecisionTreeClassifier
# 使用DecisionTreeClassifier()初始化模型
# 并设置参数max_depth=3,random_state=123，赋值给model
model = DecisionTreeClassifier(max_depth=3,random_state=123)
# 使用fit()函数训练模型
model.fit(x_train, y_train)

# 将x_test传入使用predict_proba()函数预测，将结果赋值给y_pred_proba
y_pred_proba = model.predict_proba(x_test)

# 导入sklearn.metrics模块中的roc_curve函数
from sklearn.metrics import roc_curve
# 将y_test和预测的离职概率传入roc_curve()，将结果依次赋值给fpr, tpr, thres
fpr, tpr, thres = roc_curve(y_test, y_pred_proba[:,1])
# 输出fpr, tpr, thres
print(fpr)
print(tpr)
print(thres)
```
<img width="1806" height="432" alt="image" src="https://github.com/user-attachments/assets/e467da0a-0557-415b-ae0f-e3c216c5399f" />

```python
import pandas as pd 
df = pd.read_csv("/Users/e_information/员工信息表.csv")

# 使用replace()将"工资"列的低"，"中"，"高"替换为0，1，2
df["工资"] = df["工资"].replace({'低': 0, '中': 1, '高': 2})
# 使用drop()函数删除"离职"列，剩余的数据作为自变量x
x = df.drop(columns="离职")

# 以二维结构读取"离职"列，作为因变量y
y = df[["离职"]]

# 导入sklearn.model_selection模块中的train_test_split函数
from sklearn.model_selection import train_test_split
# 使用train_test_split()函数划分训练集和测试集
x_train,x_test,y_train,y_test=train_test_split(x,y,test_size=0.2,random_state=123)

# 导入sklearn.tree模块中的分类决策树模型DecisionTreeClassifier
from sklearn.tree import DecisionTreeClassifier
# 使用DecisionTreeClassifier()初始化模型
# 并设置参数max_depth=3,random_state=123，赋值给model
model = DecisionTreeClassifier(max_depth=3,random_state=123)
# 使用fit()函数训练模型
model.fit(x_train, y_train)

# 将x_test传入使用predict_proba()函数预测，将结果赋值给y_pred_proba
y_pred_proba = model.predict_proba(x_test)

# 导入sklearn.metrics模块中的roc_curve函数
from sklearn.metrics import roc_curve
# 将y_test和预测的离职概率传入roc_curve()，将结果依次赋值给fpr, tpr, thres
fpr, tpr, thres = roc_curve(y_test, y_pred_proba[:,1])

# 导入matplotlib.pyplot，并使用"plt"作为该模块的简写
import matplotlib.pyplot as plt

# 使用plt.plot()函数，传入fpr为x轴的值，tpr为y轴的值，绘制折线图
plt.plot(fpr, tpr)

# 设置标题为ROC
plt.title('ROC')

# 设置x轴名称FPR
plt.xlabel('FPR')

# 设置y轴名称TPR
plt.ylabel('TPR')

# 使用plt.show()函数显示图像
plt.show()
```
<img width="1922" height="664" alt="image" src="https://github.com/user-attachments/assets/7c39821c-b698-479c-8709-a9af24bd9f55" />
<img width="1914" height="598" alt="image" src="https://github.com/user-attachments/assets/eee3cf9b-7f47-4ff9-bc3c-c46d6af701e1" />

+ 导入roc_auc_score函数，然后将y_test和预测的离职概率传入roc_auc_score()中，就可以直接得到AUC值。
```python
import pandas as pd 
df = pd.read_csv("/Users/e_information/员工信息表.csv")

# 使用replace()将"工资"列的低"，"中"，"高"替换为0，1，2
df["工资"] = df["工资"].replace({'低': 0, '中': 1, '高': 2})
# 使用drop()函数删除"离职"列，剩余的数据作为自变量x
x = df.drop(columns="离职")

# 以二维结构读取"离职"列，作为因变量y
y = df[["离职"]]

# 导入sklearn.model_selection模块中的train_test_split函数
from sklearn.model_selection import train_test_split
# 使用train_test_split()函数划分训练集和测试集
x_train,x_test,y_train,y_test=train_test_split(x,y,test_size=0.2,random_state=123)

# 导入sklearn.tree模块中的分类决策树模型DecisionTreeClassifier
from sklearn.tree import DecisionTreeClassifier
# 使用DecisionTreeClassifier()初始化模型
# 并设置参数max_depth=3,random_state=123，赋值给model
model = DecisionTreeClassifier(max_depth=3,random_state=123)
# 使用fit()函数训练模型
model.fit(x_train, y_train)

# 将x_test传入使用predict_proba()函数预测，将结果赋值给y_pred_proba
y_pred_proba = model.predict_proba(x_test)

# 导入sklearn.metrics模块中的roc_auc_score函数
from sklearn.metrics import roc_auc_score

# 将y_test和预测的离职概率传入roc_auc_score()，，将结果赋值给auc_score
auc_score = roc_auc_score(y_test, y_pred_proba[:,1])

# 输出auc_score
print(auc_score)
```
+ 根据输出，模型的AUC值0.9736，具有非常好的预测效果。



大多数情况下，使用模型的默认参数值，或我们手动设置的参数值，都能获得较好的预测结果。



当然，如果我们需要进一步提高模型的预测效果，我们可以进行参数调优。



最后，我们来学习参数调优的必备方法：GridSearch网格搜索，以及常与之搭配使用的K折交叉验证。
```python
import pandas as pd 
df = pd.read_csv("/Users/e_information/员工信息表.csv")

# 使用replace()将"工资"列的低"，"中"，"高"替换为0，1，2
df["工资"] = df["工资"].replace({'低': 0, '中': 1, '高': 2})
# 使用drop()函数删除"离职"列，剩余的数据作为自变量x
x = df.drop(columns="离职")

# 以二维结构读取"离职"列，作为因变量y
y = df[["离职"]]

# 导入sklearn.model_selection模块中的train_test_split函数
from sklearn.model_selection import train_test_split
# 使用train_test_split()函数划分训练集和测试集
x_train,x_test,y_train,y_test=train_test_split(x,y,test_size=0.2,random_state=123)

# 导入sklearn.tree模块中的分类决策树模型DecisionTreeClassifier
from sklearn.tree import DecisionTreeClassifier
# 使用DecisionTreeClassifier()初始化模型
# 并设置参数max_depth=3,random_state=123，赋值给model
model = DecisionTreeClassifier(max_depth=3,random_state=123)

# 导入sklearn.model_selection模块中的交叉验证的函数cross_val_score
from sklearn.model_selection import cross_val_score
# 使用cross_val_score()函数进行交叉验证，将结果赋值给auc_score
auc_score = cross_val_score(model, x_train, y_train, scoring='roc_auc',cv=5)
# 输出auc_score和平均值
print(auc_score)
print(auc_score.mean())
```
+ 第一个参数:这里的参数model，表示模型名称，即我们前面搭建的模型。
+ 第二三个参数:因为k折交叉检验，使用的是训练集数据。所以第二三个参数，分别表示训练集的自变量x_train，和因变量y_train。
+ 评估标准参数:参数scoring是用于设置评估的标准。值为'roc_auc'表示以ROC曲线的AUC值作为评估标准。
+ 交叉验证次数:cv表示交叉验证次数。不设置此参数，则默认为3次。这里设置cv=5，表示交叉验证5次。
***
+ 通常来说：当数据集较小，可以适当增大K值，保证每次迭代过程有更多的数据用于模型训练；当数据集较大，可以适当减小K值，降低模型的计算成本。
+ 计算K次效果的均值能更准确的评估模型。
+ 另一方面，它经常和﻿GridSearch网格搜索配合使用，来对模型进行参数调优
***
## GridSearch网格搜索
简单来说，GridSearch网格搜索是一种穷举搜索的参数调优手段：



自动遍历所有候选参数，循环建立模型并评估模型，选取最好的参数作为最终结果。

```python
import pandas as pd 
df = pd.read_csv("/Users/e_information/员工信息表.csv")

# 使用replace()将"工资"列的低"，"中"，"高"替换为0，1，2
df["工资"] = df["工资"].replace({'低': 0, '中': 1, '高': 2})
# 使用drop()函数删除"离职"列，剩余的数据作为自变量x
x = df.drop(columns="离职")

# 以二维结构读取"离职"列，作为因变量y
y = df[["离职"]]

# 导入sklearn.model_selection模块中的train_test_split函数
from sklearn.model_selection import train_test_split
# 使用train_test_split()函数划分训练集和测试集
x_train,x_test,y_train,y_test=train_test_split(x,y,test_size=0.2,random_state=123)

# 导入sklearn.tree模块中的分类决策树模型DecisionTreeClassifier
from sklearn.tree import DecisionTreeClassifier
# 使用DecisionTreeClassifier()初始化模型
model = DecisionTreeClassifier()

# 导入sklearn.model_selection模块中的GridSearchCV
from sklearn.model_selection import GridSearchCV
# 指定待调优参数max_depth的候选值范围，赋值给parameters
parameters = {'max_depth': [1 ,3, 5, 7, 9]}
# 使用GridSearchCV()函数进行参数调优，将结果赋值给grid_search
grid_search = GridSearchCV(model, parameters, scoring='roc_auc', cv=5)
# 传入训练集进行训练
grid_search.fit(x_train, y_train)
# 获取参数的最优值，并赋值给best_params
best_params = grid_search.best_params_
# 输出参数的最优值
print(best_params)
```
+ 待调优参数范围:
通过字典指定调优参数max_depth的候选值范围。



字典的键，为待调优参数。

字典的值，为候选值。是人为设定的一些候选值
+ 查看参数最优值:
通过属性.best_params_查看参数的最优值。
+ 给GridSearchCV()函数传入了4个参数：模型名称model；待调优参数的变量parameters；以ROC曲线的AUC值为评估标准；进行5次交叉验证。
+ 然后通过训练集数据训练，获取了参数max_depth的最优值，并输出。
+ 根据输出：{'max_depth': 7}，说明，我们将决策树的最大深度设置为7，模型的效果最优。
+ 接下来，我们试着用最优值重新搭建模型，看一看预测的效果。
```python
import pandas as pd 
df = pd.read_csv("/Users/e_information/员工信息表.csv")

# 使用replace()将"工资"列的低"，"中"，"高"替换为0，1，2
df["工资"] = df["工资"].replace({'低': 0, '中': 1, '高': 2})
# 使用drop()函数删除"离职"列，剩余的数据作为自变量x
x = df.drop(columns="离职")

# 以二维结构读取"离职"列，作为因变量y
y = df[["离职"]]

# 导入sklearn.model_selection模块中的train_test_split函数
from sklearn.model_selection import train_test_split
# 使用train_test_split()函数划分训练集和测试集
x_train,x_test,y_train,y_test=train_test_split(x,y,test_size=0.2,random_state=123)

# 导入sklearn.tree模块中的分类决策树模型DecisionTreeClassifier
from sklearn.tree import DecisionTreeClassifier

# 使用DecisionTreeClassifier()初始化模型
# 并设置参数max_depth=7,random_state=123，赋值给model
model = DecisionTreeClassifier(max_depth=7,random_state=123)

# 使用fit()函数训练模型
model.fit(x_train, y_train)

# 将x_test传入使用predict_proba()函数预测，将结果赋值给y_pred_proba
y_pred_proba = model.predict_proba(x_test)

# 导入sklearn.metrics模块中的roc_auc_score函数
from sklearn.metrics import roc_auc_score

# 将y_test和预测的离职概率传入roc_auc_score()，将结果赋值给auc_score
auc_score = roc_auc_score(y_test, y_pred_proba[:,1])

# 输出auc_score
print(auc_score)
```

+ 依次查看模型的AUC值，并格式化输出
```python
import pandas as pd 
df = pd.read_csv("/Users/e_information/员工信息表.csv")

# 使用replace()将"工资"列的低"，"中"，"高"替换为0，1，2
df["工资"] = df["工资"].replace({'低': 0, '中': 1, '高': 2})
# 使用drop()函数删除"离职"列，剩余的数据作为自变量x
x = df.drop(columns="离职")
# 将"离职"列，作为因变量y
y = df["离职"]

# 导入sklearn.model_selection模块中的train_test_split函数
from sklearn.model_selection import train_test_split
# 使用train_test_split()函数划分训练集和测试集
x_train,x_test,y_train,y_test=train_test_split(x,y,test_size=0.2,random_state=123)

# 导入sklearn.tree模块中的分类决策树模型DecisionTreeClassifier
from sklearn.tree import DecisionTreeClassifier
# for循环遍历列表[1,3,5,7,9]
for i in [1,3,5,7,9]:
    # 使用DecisionTreeClassifier()初始化模型
    # 并设置参数max_depth=i,random_state=123，赋值给model
    model = DecisionTreeClassifier(max_depth=i,random_state=123)
    # 使用fit()函数训练模型
    model.fit(x_train, y_train)

    # 将x_test传入使用predict_proba()函数预测，将结果赋值给y_pred_proba
    y_pred_proba = model.predict_proba(x_test)

    # 导入sklearn.metrics模块中的roc_auc_score函数
    from sklearn.metrics import roc_auc_score
    # 将y_test和预测的离职概率传入roc_curve()，将结果赋值给auc_score
    auc_score = roc_auc_score(y_test, y_pred_proba[:,1])
    # 格式化输出"max_depth={i}时，AUC值为{auc_score.round(4)}"
    print(f"max_depth={i}时，AUC值为{auc_score.round(4)}")
```

<img width="1233" height="2057" alt="K-MEANS算法" src="https://github.com/user-attachments/assets/6f810fda-7b5a-4f95-998a-facfbc3a7144" />
## 数据归一化
<img width="1850" height="544" alt="image" src="https://github.com/user-attachments/assets/c8f52251-3fbd-493c-8ee1-3ba4a19f4ab6" />

```python
# 导入pandas模块，简称pd
import pandas as pd

'''读取数据集并获取特征变量'''
# 读取文件，并赋值给变量df
df = pd.read_csv("/Users/user_info/new_user_info.csv")

# 获取特征变量x
x = df[["time_gap","order_count","total_amount"]]

'''数据归一化'''
# 导入sklearn.preprocessing模块中的StandardScaler类
from sklearn.preprocessing import StandardScaler

# 创建一个StandardScaler对象，并赋值给变量scaler
scaler = StandardScaler()

# 调用scaler中的fit_transform()函数，对x进行归一化，并赋值给变量x_scale
x_scale = scaler.fit_transform(x)

# 输出x_scale进行查看
print(x_scale)
```
+ 对x调用fit_transform()函数后，得到了归一化后的结果，是一个二维数组
+ 如果觉得二维数组不方便观察，可以通过pd.DataFrame()函数，传入指定的columns和index，将其转换为如图所示的DataFrame。
<img width="639" height="833" alt="20-27 (6)" src="https://github.com/user-attachments/assets/92517cd6-1971-45a2-b93e-1d827fe2c99f" />
+ 至此，我们完成了一些准备工作：读取数据集、获取特征变量，并进行数据归一化。这其实都是搭建模型中的数据预处理环节。

现在特征变量之间的**数值差距已经很小了**，接下来就可以利用K-Means算法，搭建一个全新的RFM模型啦。
## 肘部法则
<img width="1874" height="668" alt="image" src="https://github.com/user-attachments/assets/d014a7cd-58de-47eb-8c0a-f2ae4f7fdc1c" />
我们来看这幅图，当K从1增加到2、从2增加到3时，SSE的下降幅度都很大，说明增加簇的数量是有必要的。



但当K从3增加到4的时候，下降幅度明显趋向于缓慢了，说明此时增加K值已经不能让聚类的效果显著提升了，没必要再继续增加簇了。



因此，我们就取该值，也就是3，为K的值。
## 搭建K-Means模型
```python
# 导入pandas模块，简称pd
import pandas as pd

'''读取数据集并获取特征变量'''
# 读取文件，并赋值给变量df
df = pd.read_csv("/Users/user_info/new_user_info.csv")

# 获取特征变量x
x = df[["time_gap","order_count","total_amount"]]

'''数据归一化'''
# 导入StandardScaler类
from sklearn.preprocessing import StandardScaler

# 创建一个StandardScaler对象
scaler = StandardScaler()

# 对x进行归一化
x_scale = scaler.fit_transform(x)

# 将x_scale从二维数组转换为DataFrame
x_scale = pd.DataFrame(x_scale, columns=x.columns, index=x.index)

'''搭建K-Means模型'''
# 导入sklearn.cluster模块中的KMeans模型
from sklearn.cluster import KMeans

# 使用KMeans()初始化模型
# 设置参数n_clusters=3, random_state=1
# 将结果赋值给model
model = KMeans(n_clusters=3, random_state=1)

# 使用fit()函数训练模型
model.fit(x_scale)

# 获取聚类后的质心
print(f"质心：{model.cluster_centers_}")
# 获取每个样本所属的簇
print(f"标签：{model.labels_}")
# 获取SSE
print(f"SSE：{model.inertia_}")
```
+ sklearn.cluster模块，专门用于对未标记的数据进行聚类算法。
+ 使用KMeans()进行初始化，然后依次设置该模型的参数。
  + 第一个参数是n_clusters，也就是K值,这里设置为3，即将样本聚成3类，如果不设置则取默认值8。
  + 第二个参数是大家熟悉的random_state，可以保证程序每次运行的效果都一样。
+ 接下来，就可以使用fit()函数和特征变量x_scale，进行模型的训练啦～
+ 模型训练完成后，我们就可以通过访问不同的属性来获取对应的结果：
  + .cluster_centers_属性可以获取质心
  + .labels_属性可以获取**每个样本所属的簇**，也就是标签，**标签的数值对应所属簇的索引**；
  + .inertia_属性可以获取簇内误差平方和（SSE）
+ 根据.cluster_centers_属性的输出可以看到，完成聚类后，质心的值存储在一个二维数组中。



我们将K值设置为了3，所以会生成3个簇，对应3个质心。二维数组中共有3个一维数组，也就分别对应了这3个质心的值。



每一个一维数组中又有3个值，分别对应该质心3个特征变量的值，也就是归一化后的"time_gap"、"order_count"和"total_amount"的值。
<img width="660" height="212" alt="20-48 (2)" src="https://github.com/user-attachments/assets/50d906c8-214d-4cd5-b56e-3f1a90e90d0f" />
<img width="1870" height="626" alt="image" src="https://github.com/user-attachments/assets/c172bc5f-a903-491b-a21e-289c2a866635" />

***
根据肘部法则，来选择最佳的K值，即用户的类别数量。具体步骤如下：



STEP1.导入KMeans模型；

STEP2.创建一个空列表sseList，用于存储每次的SSE；

STEP3.取不同的K值，本例中我们取1至9，多次进行K-Means的聚类；

STEP4.获取当前的SSE，并使用append()函数将其添加到sseList中；

STEP5.在for循环外，输出sseList；

STEP6.绘制折线图展现结果，设置K值为x轴，sseList为y轴，marker为"o"。

```python
# 导入pandas模块，简称pd
import pandas as pd

'''读取数据集并获取特征变量'''
# 读取文件，并赋值给变量df
df = pd.read_csv("/Users/user_info/new_user_info.csv")

# 获取特征变量x
x = df[["time_gap","order_count","total_amount"]]

'''数据归一化'''
# 导入sklearn.preprocessing模块中的StandardScaler类
from sklearn.preprocessing import StandardScaler

# 创建一个StandardScaler对象
scaler = StandardScaler()

# 对x进行归一化
x_scale = scaler.fit_transform(x)

'''获取最佳K值'''
# TODO 导入sklearn.cluster模块中的KMeans模型
from sklearn.cluster import KMeans

# TODO 创建一个空列表，用于存储每次的簇内误差平方和（SSE）
sseList=[]

# TODO 使用for循环，取K值为1至9
for K in range(1,10):

    # TODO 使用KMeans()初始化模型
    # 设置参数n_clusters=k, random_state=1
    # 将结果赋值给model
    model=KMeans(n_clusters=K,random_state=1)
    
    # TODO 使用fit()函数训练模型
    model.fit(x_scale)
    
    # TODO 通过.inertia_属性获取SSE，并添加到sseList中
    sseList.append(model.inertia_)
    
# TODO 输出sseList
print(sseList)

# TODO 导入matplotlib.pyplot，并使用"plt"作为该模块的简写
import matplotlib.pyplot as plt

# TODO 使用plt.plot()函数
# 以K的取值为x轴的值，sseList为y轴的值
# 设置marker="o"，绘制折线图
plt.plot(range(1,10),sseList,marker='o')

# TODO 展示图像
plt.show()
```

+ 对K-Means算法来说，它只能帮我们划分出用户的群体，但是对于具体的某个群体属于什么样的类型，则需要我们自行分析。



通常，我们会通过散点图来展示结果，并进行分析。



本例中，我们一共有三个特征变量，分别对应R、F和M三个指标。



因此，可以绘制3D散点图，设置x轴为R、y轴为F、轴为M，来更好地展示三个用户群体的这三个指标分布

## 绘制3D散点图
```python
# 导入pandas模块，简称pd
import pandas as pd

'''读取数据集并获取特征变量'''
# 读取文件，并赋值给变量df
df = pd.read_csv("/Users/user_info/new_user_info.csv")

# 获取特征变量x
x = df[["time_gap","order_count","total_amount"]]

'''数据归一化'''
# 导入sklearn.preprocessing模块中的StandardScaler类
from sklearn.preprocessing import StandardScaler

# 创建一个StandardScaler对象
scaler = StandardScaler()

# 对x进行归一化
x_scale = scaler.fit_transform(x)

'''进行KMeans算法的聚类运算'''
# 导入sklearn.cluster模块中的KMeans模型
from sklearn.cluster import KMeans

# 使用KMeans()初始化模型
# 设置参数n_clusters=3, random_state=1
# 将结果赋值给model
model = KMeans(n_clusters=3, random_state=1)
    
# 使用fit()函数训练模型
model.fit(x_scale)

# 获取每个样本所属的簇
labels = model.labels_

'''可视化结果'''
# 导入matplotlib.pyplot，并使用"plt"作为该模块的简写
import matplotlib.pyplot as plt

# 从mpl_toolkits.mplot3d中导入Axes3D类
from mpl_toolkits.mplot3d import Axes3D

# 通过 rcParams 参数将字体设置为 Arial Unicode MS 
plt.rcParams["font.sans-serif"] = "Arial Unicode MS"

# 使用plt.figure()函数创建画布
# 添加参数figsize设置画布大小为(12,8)
fig = plt.figure(figsize=(12,8))

# 创建3D坐标轴对象
ax = fig.add_subplot(projection="3d")

# 设置散点颜色
color = ["dodgerblue", "seagreen", "lightcoral"]

# 遍历三个簇
for i in range(0,3):
    
    # 获取分类为i的点
    d = x[labels == i]
    
    # 绘制分类为i的簇所对应的R、F和M这三个指标数据
    ax.scatter(d["time_gap"], d["order_count"], d["total_amount"], color=color[i], label=f"用户群体{i}")

# 设置x轴标题为"R"
ax.set_xlabel("R")
# 设置y轴标题为"F"
ax.set_ylabel("F")
# 设置z轴标题为"M"
ax.set_zlabel("M")

# 使用plt.legend()函数展示图例
plt.legend()
# 展示图像
plt.show()
```
<img width="1442" height="1950" alt="量化交易基础（上）" src="https://github.com/user-attachments/assets/2fcf3830-5b6f-492e-b8f5-1476c05306f2" />
<img width="1465" height="1767" alt="22 量化交易基础（下）" src="https://github.com/user-attachments/assets/f2ce1d9a-4c2c-4fab-b52d-bc1a4d9faf3e" />
<img width="1439" height="1240" alt="择时策略-均线策略" src="https://github.com/user-attachments/assets/19b91a84-99a8-40a0-9579-204d905e27a6" />
<img width="1343" height="2319" alt="24 择时策略-双均线策略" src="https://github.com/user-attachments/assets/36894ee8-5158-4f81-8d05-68b0ce7b42a5" />
<img width="1289" height="2218" alt="择时策略-布林带策略" src="https://github.com/user-attachments/assets/8e2c4cd5-5db4-445f-929f-b41c948b67e3" />
























